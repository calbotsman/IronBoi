# Node / Vite
node_modules/
dist/

# macOS
.DS_Store

# Firebase Functions build output
functions/lib/

# Firebase local state + emulator logs
.firebase/
*.log
firebase-debug.log
firestore-debug.log
ui-debug.log

# Env files (commit .env.example only)
.env
.env.local
.env.*.local

# Xcode / iOS build artifacts
ios/IronBoi/.build/
ios/**/build/
ios/**/DerivedData/
**/xcuserdata/
*.xcuserstate
ios/**/Pods/
ios/**/.swiftpm/

# Phase 3 follow-up — the canonical GoogleService-Info.plist is
# generated at the start of every iOS build by project.yml's
# preBuildScripts. The committed source-of-truth files live under
# ios/IronBoi/IronBoi/Firebase/ as -Staging.plist and -Prod.plist.
ios/IronBoi/IronBoi/GoogleService-Info.plist

# Editor scratch
*.swp
.idea/
.vscode/
